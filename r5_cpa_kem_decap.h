#include "r5_cpa_kem_common.h"
void unpack_ct(uint8_t * in, uint16_t * out_0, uint8_t * out_1);
void decompress_matrix(uint8_t * in, uint16_t * out);
void _pack(uint8_t * in, uint8_t * out);
void hash(uint8_t * in, uint8_t * out);
